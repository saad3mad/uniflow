{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    // Inherit everything from the root. Add overrides here only if truly needed.
  },
  "include": [
    "**/*.ts",
    "**/*.tsx"
  ],
  "exclude": [
    "node_modules"
  ]
}
